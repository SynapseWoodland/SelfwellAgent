---
description: >
  文件与目录操作工具优先级铁则（Read / StrReplace / Write / Glob 优先，
  shell 只允许跑只读 ls/find/tree，禁止 cat/head/tail/sed/awk/echo/tee/perl 修改文件）。
  违规 shell 会被 .cursor/hooks/guard-shell.ps1 拦截 (exit 2 硬阻塞)。
alwaysApply: true
globs:
  - "**/*"
---

# 文件与目录操作工具优先级铁则

> 本文件是 alwaysApply 速查表，AI 每条消息都会看到。
> 本文件由 Cursor 规则系统根据 frontmatter 的 `alwaysApply: true` 自动注入。

本规则为最高优先级约束，必须严格遵守，不得自主选择其他工具。

## 工具优先级排序（必须从高到低选用，禁止跳级）
1. 原生文件读取工具（Read File）—— 所有文件读取场景
2. 原生增量补丁编辑工具（Apply Edits / Apply Patch）—— 局部代码修改场景
3. 原生全量文件写入工具（Write File）—— 整体重写、新建文件场景
4. 终端只读命令（ls、find、tree、mkdir、rmdir）—— 目录遍历、创建空目录场景
5. ❌ 终端文件修改命令 —— 绝对禁止

## 文件读取操作规范
- 读取文件必须使用原生 Read File 工具，禁止用 cat、head、tail 等终端命令读取文件内容。
- 修改文件前，必须先完整读取目标文件的全部内容，确认上下文后再执行修改。
- 禁止仅凭片段记忆或部分上下文直接修改文件。

## 文件修改操作规范
### 优先使用「增量补丁编辑」的场景
- 修改范围不超过文件总内容的 30%
- 仅修改局部函数、配置项、单行/多行代码片段
- 需要保留文件其余所有内容完全不变

### 使用「全量文件写入」的场景
- 文件总长度小于 200 行，且修改范围超过 50%
- 新建文件
- 整体重构文件结构
- 增量编辑连续 2 次匹配失败时，自动降级为全量写入

### 修改执行要求
- 每次修改必须生成带上下文的精准 diff，禁止模糊行号匹配。
- 一次操作尽量只修改一个文件，多文件修改需逐个执行、逐个验证。
- 修改完成后，必须重新读取文件对应片段，验证修改结果与预期一致。

## 目录操作规范
- 目录查询：仅允许使用 ls、find、tree 等只读终端命令
- 目录创建：仅允许使用 mkdir、mkdir -p
- 目录删除：仅允许删除空目录（rmdir），删除非空目录必须先向用户确认
- 禁止使用 rm -rf 等高危命令，除非用户明确书面指令

## 严格禁止的操作（红线）
以下行为在任何场景下都绝对禁止：
1. 禁止使用 sed、awk、perl、grep 等终端命令修改、替换文件内容
2. 禁止使用 echo、cat、tee 等命令通过重定向（>、>>）写入或追加文件内容
3. 禁止通过管道符组合命令间接修改文件
4. 禁止使用任何非原生编辑工具执行批量内容替换
5. 禁止在未读取完整文件的情况下直接执行修改
6. **禁止通过 PowerShell/PowerShell 脚本间接执行文件写入**（hook 只能检测 shell 命令，无法检测脚本内部操作）
7. **禁止使用 `Set-Content`、`Out-File` 等 PowerShell 命令写入含中文的文件**（PowerShell 5.x 默认 GBK 编码，会导致中文乱码）

## PowerShell 脚本文件操作禁止

### 问题背景
Windows PowerShell 5.x 的编码问题：
- `Set-Content` 默认按 `[System.Text.Encoding]::Default`（中文 Windows = GBK/CP936）写入
- UTF-8 多字节序列（中文）被 GBK 重新编码，导致乱码
- Hooks 只能检测 shell 命令，无法检测 `.ps1` 脚本内部的文件操作

### 禁止模式

| 禁止 | 原因 | 正确做法 |
|------|------|----------|
| PowerShell 脚本调用 Python 批量写文件 | 编码不可控 | 直接用 Cursor Write/StrReplace 工具 |
| `Set-Content -Path file -Value "中文内容"` | PS 5.x 默认 GBK | 用 Python `open('file', 'w', encoding='utf-8')` |
| `Out-File -FilePath file` | 默认 UTF-16 LE | 用 Python 或 `Write` 工具 |
| PowerShell 脚本里执行 `git checkout -- .` | 可能恢复暂存区的损坏版本 | 用 `git cat-file -p <hash>` 从 blob 读取 |
| `git checkout -- <file>` 在损坏后 | 恢复的是暂存区损坏内容，不是 blob | 用 `git show HEAD:<path>` 或 `git cat-file -p <blob>` |

### 正确的 UTF-8 文件写入方式

```python
# Python 永远是真 UTF-8
with open('file.md', 'w', encoding='utf-8', newline='\n') as f:
    f.write(content)

# 强制 LF 换行（避免 CRLF 问题）
content = content.replace('\r\n', '\n').replace('\r', '\n')
with open('file.md', 'wb') as f:
    f.write(content.encode('utf-8'))
```

### 从 git 恢复损坏文件的正确方式

```python
import subprocess
from pathlib import Path

def git_cat_blob(repo_root: Path, rel_path: str) -> bytes:
    """从 git blob 获取正确内容（绕过工作区损坏）"""
    # 获取 blob hash
    result = subprocess.run(
        ['git', '-C', str(repo_root), 'ls-tree', '-r', 'HEAD', '--', rel_path],
        capture_output=True
    )
    parts = result.stdout.decode().strip().split()
    if len(parts) >= 3 and parts[1] == 'blob':
        blob_hash = parts[2]
    else:
        raise ValueError(f"File not in git: {rel_path}")
    # 获取 blob 内容
    result = subprocess.run(
        ['git', '-C', str(repo_root), 'cat-file', '-p', blob_hash],
        capture_output=True
    )
    return result.stdout
```

## 文件修复标准流程
当需要修复文件问题时，严格按以下步骤执行：
1. 完整读取目标文件全部内容
2. 精确定位问题位置，分析根因
3. 优先使用增量补丁编辑工具执行修复
4. 修复后重新读取验证，确认无误再结束任务