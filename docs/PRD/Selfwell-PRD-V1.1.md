# Selfwell 产品说明书

> **版本**: V1.3
> **日期**: 2026-07-17
> **状态**: DRAFT — 等待产品评审

### 变更记录

| 版本       | 日期         | 变更类型  | 变更内容                                                                                                                                                                                                                                                                                                                                                                       |
| -------- | ---------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **V1.3** | 2026-07-17 | 功能补全  | 1. §1.4 智能管家主页新增 3 个子节：新建会话（P03a 顶栏「+」入口）、查看会话历史（P03a-History id 32）、语音识别输入（P03a-Voice id 33）；2. 同步设计稿 v1 原型（`docs/frontend-design/figma-pixso-spec/pages-v2/00-phone-prototype-v1.html`）节点 id 32/33 落地需求规格；3. 新增 voice_upload API 契约；4. 强化语音隐私合规约束（4 条红线 V-1~V-4） |
| **V1.2** | 2026-07-17 | 功能补全  | 1. 新增 §1.10 个人中心与设置页，补充抽屉管理页 8 项详细描述<br>2. 新增 §1.10「我的时光」时间轴(S10)完整说明，与心情日记的功能边界区分<br>3. 新增附录 A15 S10 个人中心与我的时光映射 |
| **V1.1** | 2026-07-17 | 基线化重写 | 1. 删除 §4 技术约束附录（技术决策编号体系全部），按业界 PRD/Tech Spec 分层标准基线化<br>2. 删除附录 A1~A14 内嵌全文，改为引用 `docs/scenarios/README.md §2` 场景总表<br>3. 精简 §0.x.4 页面架构映射，改为引用 `docs/spec/SPEC-A0-MASTER-IA.md`<br>4. 精简 §0.x.7.1 双用户旅程对照表，删除前端路由映射细节，保留业务规则<br>5. 精简 §1.6 末尾 API 契约段落，改为引用 `docs/spec/SPEC-M8-recall.md §3`<br>6. 全文字段类型描述改为业务语义描述<br>7. 在 §3.5.5 / §1.9.3 / §4.1 等删除技术内容位置添加 SPEC 引用锚点 |
| **V1.1** | 2026-07-15 | 合规修复  | 1. 新增 §0.x v2 设计稿范围说明书，建立 v2 设计与 PRD 需求的双向可追溯性<br>2. 修复 ack-pool.yaml 5条红线违规文案(id:4/9/17/26/30)，版本升至 v1.1<br>3. 新增 §3.5 心理健康危机识别与升级方案（L-Crisis 分级、升级流程、禁用回复）<br>4. 新增 RLS 迁移 `backend/alembic/versions/0009_enable_rls_all_tables.py`                                                                                                                                      |
| **V1.0** | 2026-07-15 | 初始版本  | 完整产品功能文档，9个功能主题，14个场景文档，4层合规纵深                                                                                                                                                                                                                                                                                                                                             |

> **与架构 V3 关系**: 本 PRD 独立于架构文档。架构补充见 `docs/architecture/tech-architecture-design-v3.md` 及后续 V3.1 修订版。
> **与 MVP-PRD V1.3 关系**: MVP-PRD V1.3 已作废。本 PRD 包含完整产品功能,不分 MVP/P1/P2 期。
> **PRD 版本管理**: V1.0 → V1.1(架构补充后首次修订) → V1.2(...) → **V1.1 基线化重写(2026-07-17)**
> 
> **核心业务规则(已锁,2026-07-15 评审确认)**:
> 
> - 诊断照片上传:3 张 · AI 自动识别部位 + 用户可改 · 允许部位重复 · distinct 部位数 < 3 时报告页软提示补部位
> - 诊断部位结论:AI 识别仅作 LLM 上下文,不落库
> - Day 7/14/21 主动回忆触发锚点:active plan.started_at 起算(用户在 M4 交付页点击「从今天开始」时写入);但语义要求连续打卡满 N 天才真正推送 Recall;若打卡中断但日历日期到达则不自动推送,但用户主动点「问问过去的自己」仍可用
> - 抱抱卡触发:方案进度满 N 天(plan.current_day 达到 7/14/21 **且** 当天打卡完成),里程碑式庆祝文案
> - 打卡 streak:前一天没打卡即重置为 1;补卡不补 streak(补卡只更新打卡状态 is_supplemental=true,streak 逻辑不变)
> - 方案进度展示:进度环显示 plan.current_day/21,副文案显示 streak_days
> - 方案 schema:plan_days 子表(每行 1 天,checkins FK 用 plan_day_id)，不用 plans.days 字段
> - 智能管家基线问候:规则模板池(不走 LLM)
> - 蜕变广场:S11 MVP 上线,文本发帖+基础审核,多图和主题活动 P1
> - 用户档案部位:多选数组 users.focus_parts，非单选
> - login_log 表:MVP 砍,P1 建
> - 方案天数:固定 21 天,MVP 不做长度配置
> - 打卡 streak 与方案进度:两套独立逻辑 — plan.current_day 按方案自然日走,streak_days 从第 1 个打卡日起算

---

## 0. 产品愿景

### 0.1 一句话定义

**Selfwell** 是一款 AI 驱动的自律成长陪伴产品,通过多模态 AI 诊断 + 21 天方案 + 每日打卡闭环 + 主动回忆技术,帮助用户在**不评判、不承诺效果、不量化指标**的温柔陪伴中实现自律成长。

### 0.2 目标用户

| 用户分层          | 描述                   | 核心诉求                     |
| ------------- | -------------------- | ------------------------ |
| **Primary**   | 18-35 岁女性,久坐/体态/皮肤焦虑 | 每天 10-20 分钟跟练,21 天养成自律习惯 |
| **Secondary** | 希望建立长期健康习惯,但自律动力不足   | 通过每日打卡+AI 记忆陪伴,降低放弃率     |
| **Tertiary**  | 需要"不焦虑"的自我观察工具       | 不追求"变瘦/变美",而是"与自己和解"     |

**非目标用户**:

- 需要医疗治疗的用户(产品明确拒绝)
- 追求快速见效的用户(产品无效果承诺)

### 0.3 核心问题陈述

| 维度        | 现状痛点             | Selfwell 如何解决               |
| --------- | ---------------- | --------------------------- |
| **自律工具**  | 打卡应用强迫坚持、失败=否定用户 | 温柔陪伴,失败不被评判                 |
| **诊断服务**  | 医美/护肤建议充满承诺和焦虑   | "养护方向"而非"治疗",无数字指标          |
| **AI 助手** | AI 评判用户"比之前好/差了" | Recall Safety 三层防线,绝对不评判    |
| **方案匹配**  | 方案固定,无法个性化       | AI 多模态诊断+全网视频匹配,每日动态方案      |
| **遗忘曲线**  | 21 天后忘记起点,动力消失   | Day 7/14/21 主动回忆,让用户看见自己的变化 |

### 0.4 核心价值主张

```
"和自己走过的每一天,都是进步。"
```

**Selfwell 不承诺:**

- ❌ "坚持 21 天皮肤会变好"
- ❌ "每天打卡 BMI 会下降"
- ❌ "效果比同龄人好"

**Selfwell 只承诺:**

- ✅ 每天一个专属视频方案
- ✅ Day 7/14/21 温柔回顾你的成长
- ✅ 你的数据只有你自己看得到

---

## 0.x v2 设计稿范围说明书

> **版本**: V1.0
> **日期**: 2026-07-15
> **状态**: 与 PRD V1.0 同步发布
> **设计稿位置**: `docs/frontend-design/figma-pixso-spec/pages-v2/`
> **审查文档**: `docs/design-review/PM-Review-pages-v2-20260715.md`

### 0.x.1 v2 范围概述

v2 是对 v1 设计的**合规修复 + UX 增强版**，不引入新功能。范围边界:

- **不新增**: 新功能、新页面、新用户旅程
- **仅修复**: 3 个阻断性合规违规 + 5 个建议改进
- **覆盖**: 24 个用户旅程节点(来自 `00-user-journey-v2.html`)

### 0.x.2 v2 合规修复追踪

> ⚠️ 以下 3 项为 **阻断性违规**。v2 修复在 `-v2-compliant.html` 静态文件中；v2.1 修复在 `00-olduser-journey-v2.html` 主文件中落地。
> 
> **修订记录**：
> 
> - **V1.0 §0.x.2 2026-07-15 初版** — 声明"-v2-compliant" 静态文件已修复；未在主设计稿（`00-olduser-journey-v2.html`）落地。
> - **V1.0 §0.x.2 2026-07-15 v2.1 修订** — 经设计稿逐屏核查，主文件 `00-olduser-journey-v2.html` 节点 9 / 11 / 13 / 14 仍含 v1 原文。本次按 v2.1 决策表全部替换为合规文案。修订后状态：**3/3 已修复（v2.1）**。

#### 🔴 阻断性违规 #1: 回忆页文案违规

**来源**: `08b-recall-past-self-v2-compliant.html`（v2） + `00-olduser-journey-v2.html` 节点 9（v2.1 主稿回填）
**PRD 依据**: §3.2 合规红线 #3 — 禁止前后对比判断

| 修复项  | 违规原文                 | 合规修复后               |
| ---- | -------------------- | ------------------- |
| 引导对比 | "今天感觉如何？比 3 天前好了吗？"  | "今天感觉怎么样？"          |
| 改善描述 | "肩颈：3天前『僵』 → 今天『不僵』" | "你在 7月8日 写过『肩颈有点僵』" |
| 评判性词 | "你比想象中更有韧性"          | "我们慢慢来"             |

**PRD 可追溯性**: S07 §3.5 Recall Safety ACK 模板池 + §3.2 合规红线 #3
**v2.1 落地**: `00-olduser-journey-v2.html` 节点 9（line 1041, 1048-1050 已替换为合规文本）

---

#### 🔴 阻断性违规 #2: 打卡完成页数字量化

**来源**: `08-checkin-v2-compliant.html`（v2） + `00-olduser-journey-v2.html` 节点 11（v2.1 主稿回填）
**PRD 依据**: §3.2 合规红线 #4 — 禁止数字量化承诺

| 修复项          | 违规原文                                            | 合规修复后                                         |
| ------------ | ----------------------------------------------- | --------------------------------------------- |
| 量化天数         | "已坚持 6 天"                                       | "今天的你，很好"                                     |
| 效果承诺 · 顶部小卡  | "还差 1 天就是第 1 张抱抱卡"                              | "明天是个小里程碑 🎁"                                 |
| 抱抱卡预告 · 中部大卡 | "明天可领抱抱卡 🎁 / 第 7 天 · 第 1 张抱抱卡 / 完成 Day 7 自动颁发" | "明天的你，会有新的惊喜 🌿 / 第 7 天我们一起揭晓 / 由 Day 7 自然到来" |

**PRD 可追溯性**: §1.3 每日打卡 — 打卡反馈 ACK 池 + §1.8 抱抱卡禁止表达
**v2.1 落地**: `00-olduser-journey-v2.html` 节点 11 顶部（line 1145 已替换）+ 节点 11 中部大卡（line 1176-1177 待替换）

---

#### 🔴 阻断性违规 #3: 抱抱卡文案效果承诺

**来源**: `13-hug-card-day14-v2-compliant.html` / `14-hug-card-day21-v2-compliant.html`（v2） + `00-olduser-journey-v2.html` 节点 13/14（v2.1 主稿回填）
**PRD 依据**: §1.8 抱抱卡禁止表达 + §3.2 合规红线 #2

| 节点         | 违规原文         | 合规修复后            |
| ---------- | ------------ | ---------------- |
| Day14 进度描述 | "你已超过一半"     | "我们走到了中点"        |
| Day21 收尾   | "21 天·你做到了"  | "21 天，我们一起走到了这里" |
| Day21 暗示改善 | "蜕变不是一天"     | "21 天，是一段旅程"     |
| Day14 效果承诺 | "不再靠意志力，靠习惯" | "习惯，正在慢慢形成"      |

**PRD 可追溯性**: §1.8 抱抱卡/分享海报 — 绝对禁止的表达
**v2.1 落地**: `00-olduser-journey-v2.html` 节点 13（line 1268-1269）+ 节点 14（line 1311-1312）

---

### 0.x.3 v2 设计稿验收清单

**v2 通过(18 项)**: 无需修改，可直接进入实现
**v2.1 合规修订后通过(3 项)**: §0.x.2 阻断性违规 #1/#2/#3（主设计稿文件内已替换为合规文案）

| 场景       | 节点文件                                                                         | 匹配度 | 备注                          | 状态          |
| -------- | ---------------------------------------------------------------------------- | --- | --------------------------- | ----------- |
| S02 智能管家 | `15a-butler-home-tab1.html`                                                  | 92% | 3 入口卡设计合理                   | v2 通过       |
| S03 诊断   | `01-splash.html` / `03-home.html` / `09-square.html`                         | 88% | 3 步骤清晰                      | v2 通过       |
| S04 方案   | `06-diagnosis-report.html` / `15g-p03b-diagnosis-to-plan.html`               | 85% | M4 交付页新增很好                  | v2 通过       |
| S05 打卡   | `08-checkin.html` + `00-olduser-journey-v2.html#id=11`                       | 90% | 节点 11 已 v2.1 合规修订（阻断 #2）    | **v2.1 通过** |
| S06 心情日记 | `08-checkin.html`                                                            | 95% | 完美                          | v2 通过       |
| S07 主动回忆 | `08b-recall-past-self-v2-compliant.html` + `00-olduser-journey-v2.html#id=9` | 90% | 节点 9 已 v2.1 合规修订（阻断 #1）     | **v2.1 通过** |
| S09 抱抱卡  | `12-hug-card-day7.html` + `00-olduser-journey-v2.html#id=13,14`              | 92% | 节点 13/14 已 v2.1 合规修订（阻断 #3） | **v2.1 通过** |
| S10 我的时光 | `17-record-album.html`                                                       | 95% | 完美                          | v2 通过       |
| S11 广场   | `09-square.html`                                                             | 90% | 瀑布流设计合理                     | v2 通过       |
| S12 个人中心 | `11-profile.html` ~ `22-contact.html`                                        | 92% | 4 勋章入口设计合理                  | v2 通过       |

### 0.x.4 v2 页面架构映射

> 详见 `docs/spec/SPEC-A0-MASTER-IA.md`（页面 ID 与 PRD §1 章节引用、对应场景文档、对应 PRD § 的完整映射表）

### 0.x.5 待确认事项

- [ ] **问题 #1-3**: 阻断性合规修复方案已确认(v2-compliant 文件已落地)
- [x] **ACK 模板池规模**: 已决策 — 方案 A:30 条模板池(v1.1 合规修复后)
- [ ] **空态场景**: 是否在 v2 补充 M8 回忆空态 / 今天 Tab 空态 / 我的卡片空态？

### 0.x.6 v1 → v2 变更摘要

| 变更类型    | 数量    | 说明             |
| ------- | ----- | -------------- |
| 阻断性合规修复 | 3     | 回忆页、打卡完成页、抱抱卡  |
| 建议改进    | 5     | 见 PM-Review §三 |
| 新增空态页面  | 3     | 待 PM 确认        |
| 页面覆盖    | 24 节点 | 100% 用户旅程覆盖    |

### 0.x.7 v2 → v2.1 变更摘要（2026-07-15）

> **触发原因**：v2 期间阻断性合规修复**仅落在 `-v2-compliant.html` 静态文件**，主设计稿 `00-olduser-journey-v2.html` 经逐屏核查仍含 v1 违规原文。本次 v2.1 修订把合规文本回填主设计稿。

| 变更类型       | 数量   | 说明                                        |
| ---------- | ---- | ----------------------------------------- |
| 主设计稿合规回填   | 4 节点 | 节点 9（回忆页）/ 11（打卡完成）/ 13（Day14）/ 14（Day21） |
| 隐私协议前置弹窗   | 1 节点 | 新用户节点 24 加 privacy modal，符合 PRD §1.0      |
| 误判撤回       | 1 项  | G6（节点 27 紫色卡）实际已实现，撤回补做决策                 |
| PRD 文档自身修订 | 1 项  | §0.x.2 修订注记 + §0.x.3/§0.x.4 状态同步          |

#### 0.x.7.1 双用户旅程对照表（新 ↔ 老）

> **2026-07-15 新增**：v2 启动后存在两套用户旅程稿（`00-newuser-journey-v2.html` 新用户稿 + `00-olduser-journey-v2.html` 老用户稿）。**两稿共用同一套业务节点 ID（M 系列）**，但视觉变体不同。本节精简为业务节点对照，详细路由映射见 `docs/spec/SPEC-A0-MASTER-IA.md`。

**业务节点定义**：

| 业务节点 ID | 业务概念                 | 新用户稿                   | 老用户稿         | 共用性          |
| ------- | -------------------- | ---------------------- | ------------ | ------------ |
| M1      | 启动 + 隐私协议            | 24                     | 0            | 共用后端/前端两套视觉  |
| M2      | 微信一键登录               | 25                     | —            | 仅新用户         |
| M3      | 首次引导（性别/偏好）          | 0, 1                   | —            | 仅新用户         |
| M4      | 拍照诊断                 | 2                      | —            | 仅新用户         |
| M5      | 诊断报告（智能分析）           | 3                      | —            | 仅新用户         |
| M6      | 21 天方案生成             | 4, 5                   | —            | 仅新用户         |
| M7      | 今日打卡 + 心情            | 6, 8                   | 11           | 共用后端，前端两套视觉  |
| M8      | 回忆过去（问问过去的我）         | 9                      | 9            | 共用前端 chip 组件 |
| M9      | 心情日记                 | 8                      | —（合入 M7）     | 仅新用户         |
| M10     | 抱抱卡 · Day 7          | —                      | 13           | 仅老用户         |
| M11     | 抱抱卡 · Day 14         | —                      | —（共用 M10 组件） | 仅老用户         |
| M12     | 抱抱卡 · Day 21         | —                      | 14           | 仅老用户         |
| M13     | 我的时光（相册 / 回忆）        | 7                      | 7            | 共用           |
| M14     | 广场（UGC 瀑布流）          | 27                     | 27           | 共用           |
| M15     | 智能管家 P03a Tab        | —                      | 0, 4         | 仅老用户         |
| M16-M22 | 个人中心（设置/通知/隐私/关于/联系） | 16, 18, 19, 20, 21, 22 | 同            | 共用           |
| 23      | 复发纪念弹窗（失联回访）         | —                      | 8b           | 仅老用户         |

**核心业务规则**：

1. **后端使用 M 系列 ID 作主键**（如 `journey_node_id = 'M7'`），不依赖视觉 ID。
2. **前端路由**根据 `users.lifecycle_stage` 选稿：`new` → 新用户稿，其他 → 老用户稿。
3. **共用组件**（M7/M8/M13/M14/M16-M22）抽到独立页面或组件目录，避免两稿重复维护。

#### 0.x.7.2 新用户与老用户的定义

> **2026-07-15 新增**：本节给出"新用户 / 老用户"的**语义定义与判定边界**，是 §0.x.7.3 切换规则的**前置真源**。

##### 核心定义（按 lifecycle_stage 四态）

| 用户类型      | `lifecycle_stage` | 语义定义                                                                       | 进入条件（任一）                                                      | 退出条件                                                                                           |
| --------- | ----------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| **新用户**   | `new`             | 第一次启动、刚完成 M1-M6 引导 + 诊断 + 方案生成的用户。处于"建立认知 + 形成基础方案"阶段                      | (a) 首次启动创建 user record；(b) `days_since_signup == 0 && M6 未完成` | M6 完成 → `maturing`                                                                             |
| **过渡期用户** | `maturing`        | 已完成 21 天方案生成但**尚未进入稳定期**的用户（30 天观察期）。仍在用新用户稿，但可看到部分老用户专属节点预览（M10 Day7 抱抱卡） | `M6 完成`                                                       | `days_since_signup >= 7 && completed_tasks_d1_to_d7 >= 14` → `matured` 跃迁评估通过；否则停留在 `maturing` |
| **老用户**   | `matured`         | 已度过 7 天观察期、进入"稳定陪伴 + 抱抱卡颁发"阶段的用户。走老用户稿，享 M10/M11/M12 抱抱卡 + M15 智能管家        | (a) `maturing → matured` 跃迁评估通过；(b) v1 一次性脚本回填                | 失联 ≥ 30 天 → `retained`                                                                         |
| **复发用户**  | `retained`        | 失联 30 天后回访的老用户。走老用户稿 + M23 复发纪念弹窗                                          | `now - last_active_at >= 30 days`                             | 回访当天完成任意 1 个任务 → `matured` 回迁                                                                  |

##### 判定入口字段（4 个核心维度）

| 字段       | 说明                                   | 数据来源                |
| -------- | ------------------------------------ | ------------------- |
| 用户创建时间   | 账号创建时间                               | 注册时一次性写入            |
| 当前所处阶段   | 4 选 1（new/maturing/matured/retained） | §0.x.7.3 切换规则每次启动重算 |
| 最近一次活跃时间 | 最近一次成功打卡/浏览/互动时间                     | 任何 M 节点 commit 时更新  |
| 每日打卡记录聚合 | 记录用户每天完成的任务                          | M7 打卡节点写入           |

##### 派生指标（在 backend 计算，不存库）

```
days_since_signup        = current_date - signup_at::date
completed_tasks_d1_to_d7 = COUNT(task_completion WHERE day_index BETWEEN 1 AND 7)
total_completed_tasks    = COUNT(task_completion WHERE completed_at IS NOT NULL)
days_since_last_active   = current_date - last_active_at::date
```

##### 关键边界说明

1. **"新用户"是过程状态，不是永久标签**：用户不会永远是"新用户"。新用户稿是**入门期的视觉稿**，21 天方案生成（M6）完成即失去"新用户"语义。
2. **"老用户"不是"用得久的用户"的同义词**：单纯 `days_since_signup` 大不等于老用户。**必须同时满足** `days_since_signup >= 7` **且** `completed_tasks_d1_to_d7 >= 14`。
3. **`retained` 是过渡态**：复发用户首次回访当天完成 1 个任务即回迁 `matured`，**不退化为 `maturing`**。
4. **v1 老用户回填**：v2 启动时一次性脚本把所有 v1 老用户设为 matured 阶段 + 计算 signup 天数，避免他们走新用户稿。
5. **`maturing` 观察期不弹"抱抱卡预告"**：M10 Day7 抱抱卡在 `maturing` 阶段**不出现**（PRD §1.8 抱抱卡禁止表达），只有进入 `matured` 后才进入 Day 7/14/21 颁发序列。

#### 0.x.7.3 切换判定规则（双用户旅程的核心边界）

> **2026-07-15 新增**：本节给出"何时走新用户稿 / 何时走老用户稿 / 何时弹复发纪念"的**强制判定规则**，作为后端 API、前端路由、DB 字段设计的唯一真源。

**核心字段**：`users.lifecycle_stage ∈ {new, maturing, matured, retained}`

**判定顺序**（按优先级由高到低，首条命中即终止）：

| 优先级 | 触发条件                                                       | `lifecycle_stage`         | 走哪套稿                                  | 备注                       |
| --- | ---------------------------------------------------------- | ------------------------- | ------------------------------------- | ------------------------ |
| 1   | 首次启动（无 user record）                                        | `new`                     | **新用户稿** M1-M6 全套                     | 强制走完 M6 方案生成             |
| 2   | `users.lifecycle_stage == 'new' && M6 完成`                  | `maturing`                | **新用户稿** M7-M14                       | 30 天观察期，仍走新稿 M9 等新用户专属节点 |
| 3   | `days_since_signup >= 7 && completed_tasks_d1_to_d7 >= 14` | `maturing → matured` 跃迁评估 | **老用户稿** M7/M8/M13/M14 + M10 Day7 抱抱卡 | 满足 d1-d7 完成 14/21 任务     |
| 4   | `days_since_signup >= 21 && total_completed_tasks >= 60`   | `matured`                 | **老用户稿** 完整                           | M11/M12 抱抱卡              |
| 5   | 失联 ≥ 30 天回访                                                | `matured → retained`      | **老用户稿** + **复发纪念弹窗**（视觉节点 8b）        | 弹窗基于 M9 心情日记历史选段         |
| 6   | `retained` 用户首次完成当天任务                                      | `retained → matured` 回迁   | **老用户稿**                              | 不退化为新稿                   |

> **实现约束**：
> 
> 1. **判定在后端**：`GET /api/v1/journey/next` 一次性返回 `lifecycle_stage` + `next_node_id`（M 系列 ID）+ `journey_spec_id`（`new_journey` / `old_journey` / `retained_journey`）。
> 2. **前端不再做分支判断**：拿到 `journey_spec_id` 后**直接整稿加载**，不混搭两稿节点。
> 3. **复发纪念弹窗优先级**：弹窗节点 8b 必须在 M1 之后、M15（P03a Tab）之前插入。
> 4. **数据回填**：v1 老用户一次性脚本回填 matured 阶段 + 计算 signup 天数，避免回访时走新稿。

---

## 1. 产品功能全景

> **叙述轴**:什么人 / 什么问题 / 什么价值
> 
> **功能主题划分**:9 个横切主题,每个主题对应一个或多个用户场景。每个主题包含:
> 
> 1. **问题**:这个功能解决什么问题
> 2. **核心交互**:用户怎么做,系统怎么响应
> 3. **业务规则**:必须遵守的规则
> 4. **边缘情况**:异常路径处理
> 
> **场景文档引用**:详细场景文档（含用户旅程/业务规则/边缘情况/数据契约）见 `docs/scenarios/README.md §2` 场景总表。

---

### 1.0 启动+极简登录+用户档案(场景:启动登录)

**S01 对应:详见 `docs/scenarios/S01-login-registration.md`**

#### 问题

新用户从扫码/打开 App 到首次进入 P03a 智能管家主页,需要经历:启动页 → 隐私协议 → 微信一键登录 → 5 项档案填写(可全跳)。

#### 核心交互

```
[启动页] → [隐私协议弹窗] → [微信一键登录] → [5 项档案填写] → [P03a 智能管家主页]
     ↓              ↓                ↓                ↓
  首次必现      首次必同意       微信 OAuth        可全跳
```

#### 用户路径

| 路径   | 用户类型    | 触发               | 终止                       |
| ---- | ------- | ---------------- | ------------------------ |
| 路径 A | 小程序新用户  | 扫码/分享卡进入         | 档案填写 → P03a              |
| 路径 B | 小程序老用户  | 扫码/分享卡进入         | 直接进 P03a(档案已存在)          |
| 路径 C | APP 新用户 | 首次安装启动           | 微信 OAuth → 档案 → P03a     |
| 路径 D | APP 老用户 | 启动               | 直接进 P03a(30 天登录态有效)      |
| 路径 E | 双端互通    | APP 首次启动,已有小程序账户 | unionid 关联 → 复用档案 → P03a |

#### 隐私协议前置(强制)

| 节点   | 规则                     |
| ---- | ---------------------- |
| 触发时机 | 首次启动,登录前必须弹出           |
| 用户操作 | "同意并继续"按钮,不可跳过         |
| 拒绝处理 | 点"不同意"则退出 App          |
| 版本更新 | 协议版本升级时重新弹窗确认          |
| 记录字段 | 用户 ID + 隐私协议版本 + 同意时间戳 |

> 隐私协议文本由法务提供,落地 `pages/privacy/index.wxml`;存储隐私版本 `v1.0`。

#### 登录方式

| 方式                | 入口      | 技术                                               |
| ----------------- | ------- | ------------------------------------------------ |
| **微信一键登录(小程序)**   | P01 启动页 | `wx.login()` → code 换 session_key → 后端解析 unionid |
| **微信 OAuth(APP)** | P01 启动页 | 跳转微信 → 授权回调 → 后端解析 unionid                       |
| **手机号验证码(兜底)**    | 微信登录失败后 | 发送 OTP → 验证 OTP                                  |

> MVP 不做 Apple ID / Google 登录(海外版 P2 再开)。

#### 登录态维持

| 规则               | 说明                                  |
| ---------------- | ----------------------------------- |
| JWT Access Token | 有效期 30 天                            |
| Refresh Token    | 有效期 30 天,自动续                        |
| 失效处理             | Token 过期 → 静默刷新;Refresh 也过期 → 跳转登录页 |
| 多端冲突             | unionid 唯一,双端共享同一 user_id           |

#### 双端互通规则

| 场景               | 行为                             |
| ---------------- | ------------------------------ |
| 已有小程序账户,首次用 APP  | unionid 命中 → 自动关联,复用小程序档案,静默完成 |
| 已有 APP 账户,首次用小程序 | unionid 命中 → 自动关联,复用 APP 档案    |
| 两端均有独立账户(罕见)     | unionid 冲突 → 提示用户联系客服合并        |

#### 5 项档案(可全跳)

| 字段       | 可跳  | 说明                                    |
| -------- | --- | ------------------------------------- |
| 年龄段      | ✅   | '18-22'/'23-28'/'29-35'/'36-45'/'45+' |
| 久坐程度     | ✅   | 'lt4h'/'4to8h'/'8to12h'/'gt12h'       |
| 关注部位（多选） | ✅   | 部位列表:'脸'/'肩颈'/'腰'/'背'/'腿'/'其他'        |
| 运动强度     | ✅   | '轻柔'/'适中'/'进阶'                        |
| 训练时段     | ✅   | '早晨'/'中午'/'傍晚'/'不规律'                  |

> 关注部位存储为多选数组，对应反馈记录中的部位选择场景。

#### 年龄闸门

| 规则   | 说明                            |
| ---- | ----------------------------- |
| 触发   | 注册时强制校验,未满 18 岁阻断             |
| 阻断处理 | 显示"抱歉,Selfwell 仅对 18 岁以上用户开放" |
| 校验方式 | 前端引导输入出生年月;后端计算年龄段兜底          |

#### 业务规则

| 规则             | 内容                             |
| -------------- | ------------------------------ |
| **登录失败兜底**     | 微信登录失败 → 自动切换手机号 OTP,不跳出流程     |
| **登录态 30 天**   | JWT 有效期 30 天,自动续               |
| **双端 unionid** | unionid 跨小程序/APP 唯一,走 ADR-0001 |
| **档案可跳**       | 5 项档案全部可跳过,不影响登录               |
| **隐私协议强制**     | 未同意隐私协议则不可登录                   |

#### 边缘情况

| 情况                | 处理                         |
| ----------------- | -------------------------- |
| 微信登录失败            | Toast 提示失败原因 → 自动切换手机号 OTP |
| 手机号已被占用           | 提示"该手机号已注册,请直接登录"          |
| unionid 未拿到(微信异常) | 重试 3 次 → 失败则提示稍后重试         |
| 18 岁以下用户          | 阻断,提示"仅对 18 岁以上开放"         |
| 隐私协议拒绝            | 退出 App,无法继续                |

---

### 1.1 AI 多模态诊断(场景:诊断三步卡)

**S03 对应:详见 `docs/scenarios/S03-diagnosis.md`**

#### 问题

用户在开始自律旅程前,需要了解自己的现状("我哪里最需要关注")。

#### 核心交互

```
[用户] → [上传 3 张照片] → [AI 多模态诊断] → [获得个性化报告] → [选择是否开始方案]
```

**上传 3 张照片(业务规则 F4-b + F4b1 已锁):**

- 用户上传 1~3 张人体照片(MVP 上限 3 张)
- 系统 AI 自动给每张照片识别 1 个身体部位(6 部位 + unclassified)
  - 6 部位:`脸` / `头` / `肩颈` / `腰` / `腿` / `整体气色`
  - `unclassified`:不分部位,仅心情照片允许
- 每张照片下方显示 AI 识别的部位标签 + **[修改]**按钮
- 用户可点击修改,从 6 部位下拉选择 1 个覆盖 AI 识别结果
- **允许部位重复**(例如 3 张都是脸)
- 提交后,若 distinct 部位数 < 3,报告页**软提示**"可补部位以获得更完整方案"(不阻断)
- 至少 1 张照片的部位 ≠ `unclassified` 才能提交(避免零信息)

#### AI 诊断输出

AI 诊断返回以下结构（详细数据契约见 `docs/spec/SPEC-M3-diagnosis.md §3`）：

| 字段         | 类型  | 说明                         |
| ---------- | --- | -------------------------- |
| directions | 列表  | 改善方向列表，每项含标题/描述/关联视频/严重度等级 |
| tags       | 列表  | 诊断标签列表（7-14 个）             |
| summary    | 文本  | AI 整体总结（50-100 字）          |

严重度等级含义：

| 等级     | 含义                  | 响应          |
| ------ | ------------------- | ----------- |
| **轻度** | 建议动作,自主选择           | 系统推荐视频方案    |
| **中度** | 推荐视频方案,提醒咨询专业医师     | 系统推荐 + 医疗提示 |
| **重度** | 提示"建议您咨询专业医师",不承诺效果 | 仅医疗提示,不提供方案 |

#### 诊断报告页

- 显示 3-5 条改善方向(含视频推荐)
- 显示 7-14 个诊断标签
- 显示 AI 整体总结(50-100 字)
- 每个方向显示严重度等级(`轻度/中度/重度`)

#### 业务规则

| 规则           | 内容                |
| ------------ | ----------------- |
| **照片数**      | 1-3 张,上限 3        |
| **部位识别**     | AI 自动多标签分类,用户可改   |
| **重复部位**     | 允许,distinct 计数    |
| **置信度不足**    | 软提示,不阻断           |
| **重度判定**     | 提示转医师,AI 不提供医疗建议  |
| **诊断成本**     | ≤ ¥0.15/次         |
| **诊断耗时 P95** | ≤ 20 秒(异步 SSE 流式) |
| **报告缓存**     | 7 天复用(期间重新诊断返回缓存) |

#### 边缘情况

| 情况         | 处理                             |
| ---------- | ------------------------------ |
| 上传失败       | 重试提示,最多 3 次,超时提示换网络            |
| AI 超时      | 降级到规则引擎,返回基础建议                 |
| 用户中途退出     | 不保存,重新上传                       |
| 照片不合格(非人体) | 微信 sec 校验拦截 + 提示"请上传包含人体部位的照片" |
| 用户不想做诊断    | 跳过,直接进入首页(但首页提示"先做诊断获得个性化方案")  |

---

### 1.2 21 天方案生成与视频匹配(场景:方案生成)

**S04 对应:详见 `docs/scenarios/S04-21day-plan.md`**

#### 问题

用户做完诊断后,需要一个可执行的、个性化的每日方案。

#### 核心交互

```
[诊断完成] → [方案生成中] → [21 天每日任务列表] → [每日推送视频任务]
```

#### 方案结构

| 字段    | 说明                                          |
| ----- | ------------------------------------------- |
| 方案 ID | 唯一标识符                                       |
| 用户 ID | 关联用户                                        |
| 天数列表  | 每行 1 天，含当天视频 ID 和阶段标识                       |
| 状态    | active（进行中）/ completed（已完成）/ abandoned（已放弃） |
| 开始时间  | 用户点击"从今天开始"的时间戳                             |
| 完成时间  | Day 21 打卡完成的时间戳                             |

**21 天分三阶段:**

| 阶段      | 天数        | 视频特点             |
| ------- | --------- | ---------------- |
| Phase 1 | Day 1-7   | 入门级,5-10 分钟,轻柔难度 |
| Phase 2 | Day 8-14  | 进阶,10-15 分钟,适中难度 |
| Phase 3 | Day 15-21 | 巩固,15-20 分钟,进阶难度 |

#### 视频匹配算法

```
score(video) = 0.5 × 标签匹配度 + 0.3 × 时长适配度 + 0.2 × 难度适配度

标签匹配度   = Jaccard(视频标签集, 诊断标签集)
时长适配度   = 1 - |视频时长 - 用户偏好时长| / 60
难度适配度   = 1 - |视频难度 - 用户强度等级| / 5
```

**视频池要求:**

- 启动期 ≥ 50 条视频
- 成熟期 ≥ 200 条视频
- 跳过率 > 30% 触发告警

#### 业务规则

| 规则             | 内容                                |
| -------------- | --------------------------------- |
| **方案触发**       | 诊断完成后自动生成,或用户主动进入"生成方案"页          |
| **视频匹配**       | 按诊断标签 + 用户档案(关注部位 / 运动强度)匹配       |
| **每天 1 个视频**   | 不可跳过,不可选                          |
| **已完成方案**      | 不可修改,标记 completed                 |
| **放弃方案**       | 标记 abandoned,可新建方案(新方案从 Day 1 开始) |
| **多方案**        | 最多同时 1 个 active 方案;历史方案存档         |
| **方案生成耗时 P95** | < 2 秒                             |

#### 边缘情况

| 情况                         | 处理                                        |
| -------------------------- | ----------------------------------------- |
| 视频池不足 50 条                 | 提示"方案匹配中,请稍后重试";不阻断,降级到随机推荐               |
| 21 天内提前完成(连续打卡提前到达 Day 21) | 方案提前 completed;抱抱卡 Day 7/14/21 仍按实际完成天数触发 |
| 用户中途不想做了                   | 标记 abandoned;首页引导重新生成方案                   |
| 方案生成失败                     | 降级到默认方案(Phase 1 全套入门视频)                   |

---

### 1.3 每日打卡闭环(场景:每日打卡)

**S05 对应:详见 `docs/scenarios/S05-checkin.md`**

#### 问题

用户在方案生成后,需要一个简单、低摩擦的每日打卡方式,并在完成后收到温柔的认可。

#### 核心交互

```
[每日推送提醒] → [打开 App] → [查看今日任务] → [观看视频并打卡] → [获得温柔回应]
```

#### 打卡流程

```
Step 1: 查看今日任务(视频卡片)
Step 2: 观看视频(支持视频三态降级)
Step 3: 点"完成打卡"按钮(≤ 2 步)
Step 4: 获得 AI 温柔回应(30 条 ACK 话术池随机)
```

#### 打卡卡片

```
┌─────────────────────────────────┐
│ Day 7 / 21                      │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  肩颈放松 · 晨间拉伸            │
│  [视频封面]                      │
│  5 分钟 · 轻柔                  │
│                                 │
│  [ ▶ 播放视频 ]   [ ✓ 完成打卡 ] │
└─────────────────────────────────┘
```

#### 打卡完成话术(来自 `docs/data/ack-pool.yaml` 30 条)

**触发条件**:打卡完成(心情可选填,≤ 50 字)

| 话术示例             | 出现场景 |
| ---------------- | ---- |
| "今天又迈出了一步,真的很棒。" | 任意   |
| "自律的路上,你并不孤单。"   | 任意   |
| "完成比完美更重要,你做到了。" | 任意   |

**ACK 话术禁用规则(来自 `docs/data/ack-pool.yaml`):**

- 绝对不量化("坚持了 7 天了,真棒" ❌)
- 绝对不对比("比之前好多了" ❌)
- 绝对不承诺("继续坚持皮肤会变好" ❌)
- 字数上限:30 字

#### 连续打卡中断规则

| 连续未打卡天数       | 推送话术                           |
| ------------- | ------------------------------ |
| 1 天(19:00 提醒) | "今天累了就休息,明天的你还在这里。"            |
| 2 天(10:00 提醒) | "想休息一天也可以,先把今天的你抱抱。我们慢慢来。"     |
| 3 天+          | Persona slight_hug 态,轻量关心,不强推送 |

#### streak 天数计算规则(已锁)

| 场景         | streak 值     | 说明                |
| ---------- | ------------ | ----------------- |
| Day 1 打卡   | streak = 1   | 从第 1 个打卡日起算       |
| 连续打卡       | streak 累加    | 每天 +1             |
| 前一天没打卡(中断) | streak 重置为 1 | 中断后重新从 1 开始       |
| 补卡         | streak 不补    | 补卡只更新状态,不补 streak |

> **streak 计算起点**:从用户第 1 个打卡日起算,与 plan.start_date 无关,前一天未打卡即中断,streak 归 1。

#### 补卡规则(已锁)

| 场景        | streak 值    | 说明                                 |
| --------- | ----------- | ---------------------------------- |
| 补 Day 1 卡 | streak 不补   | 补卡只更新打卡状态,不补 streak                |
| 补卡后当天打卡   | streak 正常累加 | 补卡完成后,当天打卡 streak 继续从补卡 streak 值起算 |
| 补卡后次日打卡   | streak 正常累加 | 补卡后连续打卡,streak 连续                  |

> **补卡语义**:补卡是「补记录」,不是「补坚持」。产品不评判用户「是否真的做了」,只记录用户「想要记录」。这与「不评判」价值观一致。

#### plan.current_day 与 streak_days 两套独立逻辑(已锁)

| 字段                   | 计算规则                                                | 场景示例             |
| -------------------- | --------------------------------------------------- | ---------------- |
| **plan.current_day** | 按方案自然日推进,Day 1 没打卡,Day 2 打卡,current_day = 2         | 方案到期判断(第 21 天完成) |
| **streak_days**      | 从第 1 个打卡日起算,前一天没打卡归 1,Day 1 没打卡,Day 2 打卡,streak = 1 | 抱抱卡触发/打卡连续天数展示   |

> 两套逻辑互不影响:方案进度自然走,打卡连续天数独立计算。

#### 打卡进度展示双层规则(已锁)

| 展示位置    | 字段         | 计算规则                    | 文案示例                     |
| ------- | ---------- | ----------------------- | ------------------------ |
| **进度环** | 今天 Tab 环   | `plan.current_day` / 21 | 按方案自然日推进                 |
| **副文案** | 今天 Tab 环下方 | `streak_days`           | 从第 1 个打卡日起算,前一天未打卡即重置为 1 |
| **概览卡** | 21 天方案入口卡  | `streak_days`           | 同上                       |

> **语义区分**:进度环告知「方案走到哪了」,副文案强调「你真正坚持了多久」—— 前者是事实,后者是鼓励。

#### 业务规则

| 规则             | 内容                              |
| -------------- | ------------------------------- |
| **打卡步骤**       | ≤ 2 步(看视频 + 点按钮)                |
| **心情字数**       | ≤ 50 字(可选填)                     |
| **打卡时间**       | 任意,按完成时间记                       |
| **重复打卡**       | 不允许同一用户在同一天重复打卡                 |
| **打卡反馈**       | 30 条 ACK 随机,不出现 LLM 生成话术        |
| **Persona 切换** | 按打卡状态 + 用户情绪触发 persona_state 切换 |
| **streak 计算**  | 从第 1 个打卡日起算,前一天未打卡即重置为 1        |

#### 边缘情况

| 情况          | 处理                    |
| ----------- | --------------------- |
| 视频播放失败      | 降级:标清 → 截图 → 文案兜底     |
| 用户跳过视频直接打卡  | 允许,但打卡按钮点击前须等待 ≥ 30 秒 |
| 用户已打卡但想补充心情 | 30 分钟内可编辑心情,超出不提供编辑   |

---

### 1.4 智能管家主页(场景:智能管家)

**S02 对应:详见 `docs/scenarios/S02-smart-butler.md`**

#### 问题

用户需要一个随时可交互的 AI 陪伴者,在不同场景(诊断/打卡/反馈/记忆/闲聊)下提供个性化的、不评判的陪伴。

#### 核心交互

```
[用户打开 App] → [进入智能管家主页(P03a)] → [看到入口卡] → [与 AI 对话]
```

#### P03a 主页入口卡(4 张)

| 入口卡      | 触发条件                    | 内容                |
| -------- | ----------------------- | ----------------- |
| **智能诊断** | 用户未完成诊断或超过 30 天未诊断      | "上传照片,了解现在的自己"    |
| **心情日记** | 用户进入 M7 任意路径            | "记录今天的心情,哪怕只有一句话" |
| **主动回忆** | Day 7/14/21 触发 + 用户主动入口 | "和你走过的 7 天,想看看吗?" |
| **直接输入** | 任意时刻                    | AI 对话输入框          |

#### Persona 状态机(来自 ADR-0015)

| 状态                      | 触发条件            | 话术示例                   |
| ----------------------- | --------------- | ---------------------- |
| `warm`(温暖)              | 默认              | "慢慢来,{nickname}。"      |
| `neutral`(中性)           | 闲聊/不知道说什么       | "嗯嗯,我在听。"              |
| `slight_hug`(轻度拥抱)      | 用户表达疲惫/失败/连续未打卡 | "想休息一下也没关系。"           |
| `medical_guarded`(医疗防线) | 触发医疗关键词         | "我无法回答医疗问题,建议您咨询专业医师。" |

#### Persona 状态切换规则

- **warm → slight_hug**:用户连续 7 天无反馈或连续 2 天未打卡
- **warm → medical_guarded**:用户输入含医疗/症状词(痤疮/皮肤病/药物/医生等)
- **slight_hug → warm**:用户完成打卡或提交反馈
- **medical_guarded → warm**:用户换话题

#### AI 回复规则

| 规则             | 内容                                            |
| -------------- | --------------------------------------------- |
| **回复耗时 P95**   | ≤ 3 秒                                         |
| **Persona 话术** | 4 态话术来自 Persona 合同,不出现 LLM 自由生成               |
| **合规红线**       | Recall Safety 三层防线;Persona 永不评判用户变化           |
| **未知意图**       | 走 SmartRouter unknown 路径,返回"嗯嗯,这个我还不太懂,换个话题吧" |

#### 业务规则

| 规则             | 内容                                                                                                                 |
| -------------- | ------------------------------------------------------------------------------------------------------------------ |
| **会话超时**       | 30 分钟无交互,会话自动关闭                                                                                                    |
| **同时 open 会话** | 同一用户同时最多 1 个 open session                                                                                          |
| **意图分类**       | 8 类:module_redirect / read_query / recall / recall_ack / feedback_ack / feedback_create / medical_reject / unknown |
| **会话历史**       | 30 分钟内可回溯;超出按 new session 处理                                                                                       |

#### 新建会话（P03a 顶栏「+」入口，2026-07-17 v1.3 新增）

**入口位置**：P03a 顶栏右上角「+」图标（仅当存在 open session 时显示）

**触发行为**：
```
[用户点击 +] → [二次确认弹窗"想换个话题聊聊?"] → [关闭当前 open session] → [回到首屏基线问候]
```

**业务规则**：

| 规则 | 内容 |
|------|------|
| 显示条件 | 仅当存在 open session 时显示 + 图标；首屏无会话时不显示 |
| 二次确认 | 弹窗文案"想换个话题聊聊?"（不用"重置/清空/重新开始"等评判词） |
| 数据保留 | 关闭 session ≠ 删除 session，历史会话列表仍可查看 |
| 频率限制 | 30 分钟内最多新建 3 次（防误触） |
| 影响范围 | 不影响历史会话、不影响记忆体系（短期 / 中期 / 长期记忆保留） |

**合规约束**：弹窗文案不含评判词；用户取消时回到原会话，无副作用。

---

#### 查看会话历史（P03a-History 设计稿节点 id 32，2026-07-17 v1.3 新增）

**入口位置**：P03a 顶栏左侧「≡」菜单 / 长按顶栏 / 历史会话入口卡

**展示内容**：
```
┌──────────────────────────────────────┐
│ ‹ 会话历史                              │
├──────────────────────────────────────┤
│ 🤍 今天 14:32                          │
│   "我的报告说了什么?"                      │
│   5 条消息                              │
├──────────────────────────────────────┤
│ 🤍 今天 09:15                          │
│   "今天练肩颈..."                         │
│   12 条消息                             │
├──────────────────────────────────────┤
│ 🤍 昨天 20:21                          │
│   "心情日记..."                          │
│   8 条消息                              │
└──────────────────────────────────────┘
```

**业务规则**：

| 规则 | 内容 |
|------|------|
| 列表项 | 标题（首条消息前 15 字）+ 时间（CST 时区格式）+ 消息数 |
| 排序规则 | 按 `last_active_at DESC`（最近会话在前） |
| 保留策略 | 滚动保留最近 30 天会话；超出归档（用户可见但不可点击恢复） |
| 单用户上限 | 最多保留 **50 个会话**（超出后最旧的归档） |
| 点击行为 | 加载历史消息上下文（context_window），保持对话连贯 |
| 历史会话配额 | 历史会话不计入"500 次/天"日对话上限 |
| 标题脱敏 | 自动去除医疗关键词、外貌评判词（"治疗"/"瘦了"/"好看"等） |

**API 契约**：

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/v1/assistant/sessions/history` | GET | 获取历史会话列表（分页，每页 20 条） |
| `/api/v1/assistant/sessions/{id}` | GET | 加载指定会话的完整消息记录 |

**边缘情况**：

| 情况 | 处理 |
|------|------|
| 无任何历史会话 | 显示空态："还没有对话记录，从下面的入口卡开始吧~" |
| 会话已归档 | 列表项置灰，文案"已归档"；点击提示"该对话已超出保留期限" |
| 用户注销后恢复 | 历史会话一并恢复（30 天滚动窗口） |

---

#### 语音识别输入（P03a-Voice 设计稿节点 id 33，2026-07-17 v1.3 新增）

**入口位置**：P03a 输入框右侧「🎤」麦克风图标

**交互方式**：

```
[用户按住麦克风] → [录音态：实时波形 + "正在聆听"] → [松开或自动超时] → [语音转文字] → [自动填充到输入框]
                  ↓
              [取消 / 完成]
```

**状态机**：

```
idle → recording (按下) → processing (松开) → done (填充文本) / cancel (取消)
```

**业务规则**：

| 规则 | 内容 |
|------|------|
| 单次超时 | 最长 **60 秒**，超时自动发送已识别部分 |
| 最短录音 | < 0.5 秒视为误触，不发送 |
| 识别引擎 | 微信同声传译插件（小程序）/ iOS Speech / Android SpeechRecognizer |
| 失败兜底 | 识别失败 → toast 提示"我没听清，你可以打字告诉我~" |
| 降级 | 不支持语音的端 → 隐藏麦克风图标，仅展示文字输入 |

**语音输入 vs 文本输入处理**：

| 维度 | 语音输入 | 文本输入 |
|------|----------|----------|
| 意图分类 | ✅ 识别后走相同意图分类 | ✅ 直接意图分类 |
| 合规检查 | ✅ 同 L1 输入拦截 | ✅ 同 L1 输入拦截 |
| ACK 池 | ✅ 同 ACK 池（30 字） | ✅ 同 ACK 池 |
| Persona 状态切换 | ✅ 触发 same 状态机 | ✅ 触发 same 状态机 |

**隐私合规约束**：

| 约束 | 内容 |
|------|------|
| 必须显式提示 | 录音时显示"小满正在听..."提示框（隐私法规要求） |
| 禁止后台录音 | 仅在 P03a 前台运行时允许录音；切后台立即停止 |
| 数据保留 | 语音文件保留 **7 天后自动删除**（仅保留文字记录） |
| 用户授权 | 首次使用需用户授权麦克风权限；拒绝后引导到设置 |

**API 契约**：

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/v1/assistant/voice/upload` | POST | 上传语音文件，返回识别文字 |
| `/api/v1/assistant/voice/audio` | GET | 获取语音文件播放（消息列表回放用） |

**边缘情况**：

| 情况 | 处理 |
|------|------|
| 麦克风权限被拒 | toast 提示"需要麦克风权限才能语音哦"，引导到系统设置 |
| 识别超时（> 5s） | 自动取消，提示"这次没听清，试试打字？" |
| 网络断开 | 本地暂存录音，恢复后重试 |
| 静默录音（无声音） | < 0.5 秒自动取消 |
| 嘈杂环境 | 提示"换个安静的环境试试？" |

**合规红线（语音专属）**：

| 红线 | 内容 |
|------|------|
| V-1 | 永不静默录音（必须用户主动按下） |
| V-2 | 永不后台录音（切后台立即停止） |
| V-3 | 永不上传非用户主动发起的语音 |
| V-4 | 语音内容走相同 LLM 合规四层纵深 |

---

#### 基线问候规则模板池(不走 LLM,业务规则已锁)

| 档位             | 触发条件          | 模板文案示例                         |
| -------------- | ------------- | ------------------------------ |
| (a) 未开始        | 用户未做诊断        | "早上好,我是 Selfwell。今天想做什么?"      |
| (b) 已诊断+今日打卡完成 | 已诊断 + 今日已完成打卡 | "今天练完了,{nickname} 真的可以。我们明天见。" |
| (c) 今日未打卡      | 今日未打卡         | "今天累了就休息,明天的你还在这里。"            |
| (d) 连续 ≥2 天未打卡 | 前一天+更早均未打卡    | "想休息一天也可以,先把今天的你抱抱。我们慢慢来。"     |

> 模板来源:待建 `docs/data/greeting-pool.yaml`,由 S02 场景废弃的 LLM Prompt 改写。

---

### 1.5 心情日记 + 多部位反馈(场景:反馈)

**S06 对应:详见 `docs/scenarios/S06-feedback.md`**

#### 问题

用户想记录自己的心情或身体状态,不只是打卡。心情日记是可选的,但 AI 会温柔回应。

#### 核心交互

```
[用户进入心情日记页] → [选择心情类型] → [上传照片或文字] → [可选选择部位] → [获得温柔回应]
```

#### 4 种心情日记类型(来自 ADR-0016)

| feedback_type        | 必填字段        | 可选字段     | 场景            |
| -------------------- | ----------- | -------- | ------------- |
| `mood_text`          | 文字内容        | —        | 纯文字记录         |
| `mood_photo`         | 照片          | 文字内容(可选) | 带图的日常记录       |
| `period_photo`       | 照片 + **部位** | —        | 周期对比照片(须指定部位) |
| `plan_compare_photo` | 照片 + **部位** | —        | 21 天方案完成态对比照片 |

#### 部位选择(业务规则 F4-b)

- `period_photo` / `plan_compare_photo` **必须选择部位**
- `mood_photo` **可选部位**
- 部位枚举:`脸` / `头` / `肩颈` / `腰` / `腿` / `整体气色`
- 部位由用户从 6 部位下拉选择,**AI 不自动修改**(与诊断照片不同,此处是用户主动选择)

#### AI ACK 回应

- 每条 feedback 触发 1 条 ACK 话术(从 30 条池随机)
- 字数上限:30 字
- **不重复**:连续 3 条 feedback 不出现相同 ACK
- **不生成**:ACK 绝对不调用 LLM,只从固定池取

#### 业务规则

| 规则         | 内容                                                         |
| ---------- | ---------------------------------------------------------- |
| **4 种类型**  | mood_text / mood_photo / period_photo / plan_compare_photo |
| **部位选择**   | period_photo / plan_compare_photo 必填;其它可选                  |
| **文字内容字数** | ≤ 500 字                                                    |
| **多部位**    | MVP 单条 feedback 只能选 1 个部位;多部位反馈 P2+ 扩展                     |
| **ACK 字数** | ≤ 30 字                                                     |
| **照片上传**   | 复用 1.1 诊断上传流程(AI 自动识别部位)                                   |

#### 边缘情况

| 情况                 | 处理                            |
| ------------------ | ----------------------------- |
| period_photo 未选部位  | 阻断提交,提示"请选择部位"                |
| mood_photo 上传后想改部位 | 支持在提交前修改;提交后不可改               |
| 用户连续 3 条 feedback  | 第 3 条触发 Persona slight_hug 切换 |

---

### 1.6 主动回忆 Day 7/14/21(场景:主动回忆)

**S07 对应:详见 `docs/scenarios/S07-recall.md`**

#### 问题

21 天自律旅程中,用户在第 7/14/21 天需要一个温柔的"回头看",帮助他们看见自己的变化,而不是焦虑于"还剩多少天"。

#### 核心交互

```
[Day N 早上 8:00] → [系统自动加载反馈素材] → [AI 生成回忆卡] → [推送通知]
         ↓
    [用户打开 App] → [主动回忆页面] → [看到温柔的回顾] → [获得鼓励语]
```

#### Day N 触发规则(D1 已锁:仅对 active plan 触发)

**触发锚点**:active plan.started_at 起算

- Day 7:plan.started_at + 7 天,08:00 触发
- Day 14:plan.started_at + 14 天,08:00 触发
- Day 21:plan.started_at + 21 天,08:00 触发

> **started_at 语义**:用户在 M4 交付页点击「从今天开始」按钮时写入,强调「你真正开始行动的那天」,不是「方案生成的那天」。若用户跳过 M4 直接进入 App,started_at = now()。

**触发条件(双层)**:

1. **日期条件**:方案状态为进行中 + 今天 >= 方案开始时间 + N 天
2. **打卡条件**:用户**连续打卡满 N 天**(从第 1 个打卡日起算,前一天没打卡即中断归 1)
   - 若打卡中断但日历日期到达:Recall 气泡**不自动推送**
   - 但用户主动点「问问过去的自己」**不受此限制**,实时生成

**未打开 App 的处理**:

- 当日 10:00 再推送 1 次提醒
- 若用户仍未打开:不重复推送,当天 20:00 静默;回忆卡在 App 内"待看"状态保留 7 天

#### 回忆素材来源

| 素材    | 来源              | 说明                             |
| ----- | --------------- | ------------------------------ |
| 文字心情  | 心情日记文字类型        | 用户写的感想                         |
| 周期照片  | 心情日记周期照片/方案对比照片 | 带部位的照片                         |
| 打卡记录  | 打卡完成记录          | 已完成的视频任务                       |
| 非评判摘要 | AI 从以上素材生成      | 总结"这 N 天你关注了什么,感觉如何",不提"改善/变化" |

#### Recall Safety 三层防线(来自 ADR-0017)

| 层       | 防线内容       | 说明                                                    |
| ------- | ---------- | ----------------------------------------------------- |
| Layer 1 | Prompt 硬编码 | AI 绝对不评判用户前后变化/颜值/数字                                  |
| Layer 2 | 100+ 敏感词库  | 4 组:前后评判(50+词) / 效果承诺(22+词) / 数字评判(15+词) / 外貌评判(20+词) |
| Layer 3 | 安全兜底       | 命中敏感词时,替换为 5 条 safe_fallback 随机                       |

**敏感词库真源**:`docs/data/recall-forbidden-words.yaml`

**绝对禁止的词(示例):**

```
进步/变好/瘦了/胖了/颜值/体重/三围/变漂亮了/比我好...
```

**安全兜底示例:**

- "自律是一辈子的事情,不在于某一天的完美。"
- "每个人的节奏不一样,不必和别人比较。"

#### 业务规则

| 规则               | 内容                                         |
| ---------------- | ------------------------------------------ |
| **触发锚点**         | active plan.started_at 起算                  |
| **触发时间**         | 08:00(仅 active plan)                       |
| **ai_summary**   | ≤ 200 字,不提变化/数字                            |
| **ai_encourage** | ≤ 80 字,温柔鼓励                                |
| **Safety 通过**    | 100+ 敏感词 3 层全过,方能推送给用户                     |
| **素材缺失**         | 不足 3 条 feedback 时,补充系统打卡摘要填充               |
| **无素材**          | 无任何 feedback + checkin → 显示默认回忆卡(不暴露零数据尴尬) |

#### 边缘情况

| 情况                               | 处理                           |
| -------------------------------- | ---------------------------- |
| Day 7 触发但用户第 3 天才建 plan          | 实际建 plan 后 + 7 天才触发(不是注册时间)  |
| Day 14 触发但用户提前完成方案(连续打卡到 Day 14) | 按实际完成天数触发,不提前                |
| 用户主动进入 Day N 回忆入口                | 不受 08:00 限制,实时生成,同 Safety 检查 |
| 用户注销后恢复                          | 恢复 active plan,继续累计          |

#### 自定义时间跨度（用户主动"问问过去"）

> **2026-07-15 v1.0 新增**：用户主动点「问问过去」时，UI 给出**默认快捷选项 7 天 / 14 天 / 21 天** + **自定义 chip 入口**。本节规定自定义跨度的边界规则。

**前端入口**：`00-olduser-journey-v2.html` 节点 9 回忆页快捷 chip 数组：`switchAskPast(3|7|14|21|...)`，3 与自定义边界由本节 CSR-1 ~ CSR-8 约束。

> **v2.1 修订注**：设计稿节点 9 快捷 chip 实际包含 **3 / 7 / 14 / 21 + 自定义**（不只是 7/14/21）。3 天 chip 是面向"刚完成 Day 3 的新用户"的近跨度入口；自定义 chip 承担 1 ~ 99 天任意跨度。

| 规则编号  | 规则      | 边界值                       | 后端含义                                                 |
| ----- | ------- | ------------------------- | ---------------------------------------------------- |
| CSR-1 | 时间跨度下界  | `>= 1` 天                  | `recall_span_days >= 1`                              |
| CSR-2 | 时间跨度上界  | `<= plan.current_day`     | 若 `plan.current_day=10`，用户最多问"10 天前"，**问 20 天前直接拒绝** |
| CSR-3 | 计划不存在   | —                         | 方案状态非进行中 → 返回错误码，UI 提示"还没有开始的日子可以回忆"                 |
| CSR-4 | 反馈数据不足  | —                         | 该跨度区间内 feedback < 3 条 → 触发默认摘要兜底（同 §1.6 "无素材" 行为）    |
| CSR-5 | AI 输出长度 | 同 §1.6 ai_summary ≤ 200 字 | Recall Safety 三层防线 100% 走全                           |
| CSR-6 | 触发频率限流  | 单用户 ≤ 5 次/24 小时           | 24h 滑动窗口计数                                           |
| CSR-7 | 时区      | 按用户所在时区计算"今天 0 时"         | 后端用用户时区（如缺失则 `Asia/Shanghai`）                        |
| CSR-8 | 数据采样    | 区间内随机 ≤ 20 条 feedback     | 防 prompt 过长                                          |

**API 契约**（详细数据契约见 `docs/spec/SPEC-M8-recall.md §3`）：

| 端点     | 说明                                                |
| ------ | ------------------------------------------------- |
| 7 天回忆  | GET /api/v1/recall?span_days=7                    |
| 14 天回忆 | GET /api/v1/recall?span_days=14                   |
| 21 天回忆 | GET /api/v1/recall?span_days=21                   |
| 自定义跨度  | GET /api/v1/recall?custom_span_days=10（上限为当前方案进度） |

成功响应包含:ai_summary（≤200字）/ ai_encourage（≤80字）/ span_days / plan_current_day / feedback_count

---

### 1.7 蜕变广场社区(场景:蜕变广场)

**S11 对应:详见 `docs/scenarios/S11-plaza.md`**

#### 问题

用户需要一个"看见同行者"的社区,不是攀比,而是"原来不只是我一个人"。

#### 核心交互

```
[用户发帖] → [三段式审核] → [通过后进广场] → [其他人可见]
```

#### 发帖规则

| 规则       | 内容                                  |
| -------- | ----------------------------------- |
| **内容字数** | ≤ 200 字                             |
| **图片**   | ≤ 9 张                               |
| **审核状态** | `pending` → `approved` / `rejected` |

#### 三段式审核流程(来自架构 V3 §3.5)

```
Step 1: 关键词拦截(无 AI)
         ↓ 通过
Step 2: 采样 LLM 分类(10% 异步)
         ↓ 通过
Step 3: 人工队列(高风险/申诉)
         ↓
    [发布] 或 [拒稿 + 原因]
```

#### 社区内容规则

- **无点赞功能**(MVP 砍点赞,只展示)
- **AI 暖心评论**:审核通过后,AI 自动生成 1 条暖心评论(30 条 ACK 话术池)
- **官方精选**:运营可手动加官方评语
- **动态字数上限**:≤ 200 字

#### 边缘情况

| 情况     | 处理                                    |
| ------ | ------------------------------------- |
| 审核拒绝   | 通知用户 + 拒绝原因(不显示"AI 拒绝")               |
| 申诉     | 用户可申诉 1 次,进入人工队列                      |
| 违规内容漏审 | Layer 4 异步复审(10% 采样)发现后,下架 + 记录合规审计日志 |

---

### 1.8 抱抱卡/分享海报(场景:抱抱卡)

**S09 对应:详见 `docs/scenarios/S09-hug-card.md`**

#### 问题

用户在 Day 7/14/21 完成打卡后,获得一张温暖的分享海报,可以分享到朋友圈/微信,带来社交传播。

#### 核心交互

```
[Day 7/14/21 打卡完成] → [生成抱抱卡海报] → [用户选择分享/保存]
```

#### 抱抱卡文案规则(来自 SPEC-M10 禁止词)

**允许的表达:**

- "和你走过的 {n} 天"
- "和你一起 · 第 {n} 天"
- "我们一起走到了这里"
- "我们走到了中点"（Day 14 专用）

**绝对禁止的表达:**

- ❌ "我坚持了 7 天"
- ❌ "已坚持 21 天"
- ❌ "效果比之前好多了"
- ❌ "皮肤变好了"
- ❌ "已坚持 {streak_days} 天"（抱抱卡不用 streak_days,用方案进度）

> **抱抱卡语义**:里程碑式庆祝 —— 「我们一起走到了这里」,强调「陪伴」而非「坚持」。文案基于方案进度(plan.current_day),而非打卡连续天数(streak_days)。

#### 抱抱卡内容

| 内容   | 来源                                                |
| ---- | ------------------------------------------------- |
| 分享文案 | AI 生成(Recall Safety 检查后),基于 user.nickname + day 数 |
| 配图   | 用户方案对比照片(优先);无则用心情照片                              |
| 视频截图 | 当天打卡视频封面                                          |
| 二维码  | 小程序码(带用户参数)                                       |

#### 业务规则

| 规则       | 内容                                                              |
| -------- | --------------------------------------------------------------- |
| **触发点**  | Day 7 / Day 14 / Day 21 **打卡完成后**                               |
| **触发条件** | plan.current_day 达到 7/14/21 **且** 当天打卡完成;用户有方案对比照片;无则降级到普通打卡完成卡 |
| **海报尺寸** | 适配朋友圈 3:4 比例                                                    |
| **有效期**  | 永久有效                                                            |
| **分享方式** | 微信小程序分享(原生能力)                                                   |

> **触发语义**:抱抱卡是里程碑庆祝,「我们一起走到了这里」。方案进度满 N 天(plan.current_day)即触发,不要求连续打卡。打卡中断用户依然可获得抱抱卡,只是文案会温柔一些。

---

### 1.9 用户账户、合规与数据管理

#### 1.9.1 用户注册与登录

| 场景     | 说明                                           |
| ------ | -------------------------------------------- |
| 登录方式   | 微信小程序登录(UnionID)                             |
| 可选档案字段 | 年龄段 / 久坐程度 / 关注部位 / 运动强度 / 训练时段；全部可选,登录时引导填写 |
| 隐私协议   | 首次登录强制展示,同意后进入引导页                            |

#### 1.9.2 推送调度

**S13 对应:详见 `docs/scenarios/S13-notification.md`**

| 推送类型             | 触发时间                      | 通道           |
| ---------------- | ------------------------- | ------------ |
| 每日打卡提醒           | 用户训练时段对应时间                | 微信订阅消息(兜底邮件) |
| Day 7/14/21 回忆提醒 | 08:00                     | 微信订阅消息       |
| 连续未打卡关心          | Day 1 19:00 / Day 2 10:00 | 微信订阅消息       |
| 社区审核结果           | 实时                        | 微信订阅消息       |

#### 1.9.3 数据导出(PIPL/GDPR)

**S12 对应:详见 `docs/scenarios/S12-profile-export.md`**

> **技术实现**:数据导出完整 API 契约与数据格式规范见 `docs/spec/SPEC-M12-timezone-cst-display.md §X.3`。

| 规则          | 内容                                                 |
| ----------- | -------------------------------------------------- |
| **触发**      | 用户在个人中心点击"导出我的数据"                                  |
| **方式**      | 异步 Job,秒回 job_id                                   |
| **格式**      | ZIP 包(含 JSON 格式数据 + 用户上传照片)                        |
| **URL 有效期** | 7 天过期                                              |
| **下载限制**    | 单用户,单次下载                                           |
| **审计**      | 记录合规审计日志（详见 `docs/spec/SPEC-M14-compliance.md §5`） |

#### 1.9.4 账户注销

| 规则       | 内容                       |
| -------- | ------------------------ |
| **冷静期**  | 15 天                     |
| **数据冻结** | 注销发起后,所有数据冻结,不可登录        |
| **恢复**   | 冷静期内点击"恢复账户"             |
| **永久删除** | 冷静期结束:用户数据/向量/对象文件全部物理删除 |
| **GDPR** | 注销后可申请即时删除(特殊通道)         |

---

### 1.10 个人中心与设置页(场景:个人中心)

**S10 对应:详见 `docs/scenarios/S10-my-timeline.md`**

#### 问题

用户需要一个集中的入口查看自己的成长轨迹、管理设置、了解产品信息。

#### 核心交互

```
[点击底部导航「我的」] → [个人主页] → [点击各入口卡片] → [进入对应页面]
```

#### 个人主页结构

| 区域 | 内容 |
|------|------|
| 顶部 Profile | 头像 + 昵称 + 方案进度徽章 |
| 快捷入口 | 记录相册(照片统计) + 勋章入口 |
| 设置列表 | 抽屉管理页入口 |

#### 抽屉管理页(8 项)

抽屉从「今天」Tab 右下角入口展开，8 个管理入口:

| 入口卡片 | 跳转页面 | 核心功能 |
|---------|---------|---------|
| **用户档案** | `/pages/profile-edit` | 查看/编辑个人信息、关注部位、运动强度等 |
| **我的时光** | `/pages/record-album` | 21 天时间轴 + 打卡里程碑节点 |
| **通知设置** | `/pages/notification-settings` | 打卡提醒时段、Recall 提醒开关 |
| **隐私政策** | `/pages/privacy-policy` | 隐私协议 + 数据导出 + 账户注销 |
| **联系客服** | `/pages/contact` | 常见问题 + 在线客服入口 |
| **关于自愈** | `/pages/about` | 产品介绍 + 版本信息 |
| **心情日记** | `/pages/feedback-diary` | 快速记录心情(详见 §1.5) |
| **21 天方案** | `/pages/plan-delivery` | 方案日历 + 阶段进度(详见 §1.2) |

#### 「我的时光」时间轴(S10 时光相册)

**核心价值**:帮助用户看见自己走过的每一天，"看看我走过的路"。

| 功能 | 说明 |
|------|------|
| **时间轴视图** | 21 天纵向时间轴，每节点 = 1 天 |
| **节点内容** | Day N + 任务名称 + 完成状态 + 心情照片标记 |
| **里程碑节点** | Day 7 / 14 / 21 特殊样式标记 |
| **入口** | 抽屉「我的时光」/ 首页 time-section / 个人中心勋章区(3 处) |

**与心情日记的区别**:

- 「我的时光」= 回顾(read-only，看时间轴)
- 「心情日记」= 记录(write，创建新内容)

#### 业务规则

| 规则 | 内容 |
|------|------|
| **抽屉入口** | 仅老用户可见(`lifecycle_stage != 'new'`) |
| **我的时光入口** | 首页 time-section + 抽屉 + 个人中心勋章区(3 处) |
| **照片隐私** | 所有照片仅用户本人可见 |
| **照片清除** | 诊断原图 7 天后自动清除(详见 §3.3) |

#### 边缘情况

| 情况 | 处理 |
|------|------|
| 无打卡记录 | 显示空态引导"开始你的第一次打卡" |
| 方案未开始 | 「我的时光」入口灰置，提示"开始方案后可见" |
| 抽屉项页面未实现 | 降级为 Toast 提示"功能即将上线" |

---

## 2. 用户旅程

### 2.1 新用户旅程(首次打开 App)

```
[打开小程序] → [隐私协议弹窗] → [同意] → [微信授权登录]
    ↓
[4 屏引导页](极简介绍:什么是 Selfwell / 拍照即诊断 / 21 天陪伴 / 温柔不评判)
    ↓
[引导做诊断] → [上传 3 张照片] → [AI 诊断报告] → [选择开始方案]
    ↓
[21 天方案] → [每日推送提醒] → [每日打卡] → [Day 7/14/21 主动回忆]
    ↓
[21 天完成] → [抱抱卡 Day 21] → [可选:新一轮方案]
```

### 2.2 老用户旅程(已做过诊断)

```
[打开 App] → [智能管家主页] → [看入口卡状态]
    ↓
[智能诊断卡] → [30 天内未诊断] → [提示"好久没诊断了,了解下新的自己?"]
[mood_diary 卡] → [快速记录心情/照片]
[主动回忆卡] → [Day 7/14/21 触发时可见]
[直接输入] → [和 AI 对话]
```

### 2.3 异常旅程

| 场景       | 用户路径                                    |
| -------- | --------------------------------------- |
| 诊断后不想做方案 | 跳过 → 进入首页(引导做诊断,但不强制)                   |
| 方案中途放弃   | abandoned 方案 → 首页引导重新生成方案               |
| 连续未打卡    | Persona slight_hug 态 → 轻量关心推送           |
| 问医疗问题    | Persona medical_guarded 态 → "建议您咨询专业医师" |

---

## 3. 合规与隐私

> **叙述轴**:什么人 / 什么问题 / 什么价值
> **合规目标**:让用户感受到"我的数据是安全的",而不是"我们被监管了"

### 3.1 合规四层纵深(产品规则层)

| 层   | 名称     | 产品表现                                     |
| --- | ------ | ---------------------------------------- |
| L1  | 输入拦截   | 微信 sec 校验 + 关键词拦截(医疗/功效承诺词)              |
| L2  | LLM 约束 | System Prompt 硬编码合规红线 + JSON Schema 约束输出 |
| L3  | 输出网关   | 逐 chunk 关键词检查 + 采样 LLM 分类                |
| L4  | 异步复审   | 社区内容三段式审核(关键词/采样 LLM/人工) + 合规审计日志记录      |

### 3.2 合规红线(产品面向用户的话术规则)

| 红线  | 禁止                   | 替代                |
| --- | -------------------- | ----------------- |
| 1   | 医疗表述(治疗/治愈/医生/处方/病症) | "养护方向" / "生活方式建议" |
| 2   | 效果承诺(会变白/会变小/会提升)    | 不承诺,只说"建议"        |
| 3   | 前后对比判断(比之前好/进步了)     | "你的感受很重要"         |
| 4   | 数字量化(BMI/体重/三围)      | 不量化               |
| 5   | 颜值/外貌评判(变漂亮/颜值高)     | 不评判               |
| 6   | 强推打卡("必须坚持")         | "今天做了就很棒"         |

### 3.3 隐私保护原则

| 原则           | 内容                              |
| ------------ | ------------------------------- |
| **逐支用户隐私隔离** | 用户上传的照片只有本人可见                   |
| **数据最小化**    | 诊断原图 7 天后自动清除(保留 AI 诊断结论,不保留原图) |
| **透明告知**     | 隐私协议首次登录强制展示                    |
| **用户控制权**    | 可导出数据、可注销账户(15 天冷静期)            |
| **无第三方分享**   | 用户数据不卖给/分享给任何第三方                |

### 3.4 安全网关(产品承诺层)

| 承诺       | 说明                     |
| -------- | ---------------------- |
| **上传安全** | 微信 sec 校验 + 内容安全 API   |
| **传输安全** | HTTPS 加密传输             |
| **存储安全** | 私有桶,签名 URL 访问          |
| **访问控制** | JWT Token + UnionID 鉴权 |

### 3.5 心理健康危机识别与升级

> **背景**:Selfwell 目标用户(18-35 岁女性,体像焦虑)是高心理健康风险群体,产品必须具备危机识别与专业资源转介能力,这既是伦理责任,也是 PIPL/网络安全法的潜在要求。

#### 3.5.1 危机分级

| 级别                   | 触发条件                              | 产品响应                  |
| -------------------- | --------------------------------- | --------------------- |
| **L-Crisis** (立即危机)  | 用户表达自杀意念/自伤行为("活着没意义"、"想死"、"割腕"等) | 停止一切 AI 交互,立即展示危机资源卡  |
| **L-Serious** (严重情绪) | 用户表达持续抑郁/无助/绝望情绪,且无明确触发事件         | AI 暂停提供建议,展示温柔倾听+专业资源 |
| **L-Medical** (医疗急迫) | 用户表达急性心理症状(惊恐发作、解离等)              | 建议立即就医,提供急诊指引         |

#### 3.5.2 L-Crisis 升级流程(立即执行)

```
[用户输入触发 crisis 关键词]
    ↓
[AI 立即停止生成 → 展示危机响应卡]
    ↓
┌─────────────────────────────────────────┐
│  🌿 我在这里陪着你                           │
│                                          │
│  如果你现在感到很难熬,                        │
│  请不要一个人扛着。                          │
│                                          │
│  📞 全国心理援助热线: 400-161-9995           │
│     (24 小时免费)                          │
│                                          │
│  📞 北京心理危机研究与干预中心: 010-82951332  │
│     (24 小时)                             │
│                                          │
│  🚨 如有紧急危险,请立即拨打 120 或 110        │
│                                          │
│  [我需要帮助]  [我只是想聊聊]               │
└─────────────────────────────────────────┘
    ↓
["我只是聊聊" → 恢复轻度 AI 陪伴,但继续监控输入]
["我需要帮助" → 展示完整资源列表,不恢复 AI 对话]
```

#### 3.5.3 L-Crisis 禁用回复(绝对禁止)

以下回复模式在 crisis 场景下**绝对禁止**:

- ❌ "我无法回答医疗问题,建议您咨询专业医师"(冷漠、不即时)
- ❌ "建议您拨打心理援助热线"(单一资源、无情感连接)
- ❌ 任何 AI 生成的建议、分析、或共情长文(可能加重危机)
- ❌ 任何形式的打卡提醒或方案推送

#### 3.5.4 危机词表管理

危机词表(`docs/data/mental-health-crisis-words.yaml`)独立于 `medical-reject-words.yaml` 管理:

| 词表                                                | 用途         | 严重级别              |
| ------------------------------------------------- | ---------- | ----------------- |
| `mental-health-crisis-words.yaml`                 | 自杀/自伤/危机表达 | **L-Crisis** (最高) |
| `medical-reject-words.yaml` §dangerous_behavior   | 重度心理/精神症状  | L-Serious         |
| `medical-reject-words.yaml` §medication_treatment | 医疗/用药咨询    | L-Medical → 医疗转介  |

#### 3.5.5 数据记录

> **技术实现**:合规审计日志完整字段定义见 `docs/spec/SPEC-M14-compliance.md §5`。

危机事件按以下规则处理:

| 规则          | 内容                                    |
| ----------- | ------------------------------------- |
| **不记录对话内容** | 用户在危机状态下的输入不存储于 AI 对话历史               |
| **匿名统计**    | 仅记录"某日触发 L-Crisis N 次",用于产品安全监测       |
| **审计日志**    | 合规审计日志表记录 crisis 触发事件(不含对话内容),保留 90 天 |

#### 3.5.6 心理健康合规对齐

| 监管要求               | 对齐措施                      |
| ------------------ | ------------------------- |
| PIPL Art.13 — 同意   | 用户协议中明确告知心理健康支持功能的存在与限制   |
| PIPL Art.14 — 敏感信息 | 危机词表数据不关联用户身份标识           |
| 《互联网信息服务深度合成管理规定》  | AI 对话中明确"我不是专业心理咨询师"的身份声明 |
| 行业伦理               | 产品团队应定期审查危机词表,建议每季度一次     |

---

## 4. NFR 可承诺项

### 4.1 LLM 成本控制承诺

| 承诺       | 值       | 说明           |
| -------- | ------- | ------------ |
| 月 LLM 预算 | ≤ ¥700  | 连续超预算触发降级链   |
| 单次诊断成本   | ≤ ¥0.15 | 多模态 LLM      |
| 单次对话成本   | ≤ ¥0.02 | 文本 LLM       |
| 基线问候成本   | ¥0      | 规则模板池,不走 LLM |
| 日 LLM 预算 | ≤ ¥40   | 熔断阈值         |

---

## 附录:场景详细文档索引

> **说明**:以下 15 份场景文档原文均独立存放于 `docs/scenarios/` 目录,不再内嵌于 PRD 本文中。场景编号与 PRD §1 章节的映射关系见 `docs/scenarios/README.md §2` 场景总表。

| 附录  | 场景编号 | 场景名称             | PRD 映射章节        |
| --- | ---- | ---------------- | --------------- |
| A1  | S01  | 启动+登录+档案         | §1.0            |
| A2  | S02  | 智能管家主页           | §1.4            |
| A3  | S03  | 诊断三步卡            | §1.1            |
| A4  | S04  | 21 天方案           | §1.2            |
| A5  | S05  | 每日打卡             | §1.3            |
| A6  | S06  | 心情日记+多部位反馈       | §1.5            |
| A7  | S07  | 主动回忆 Day 7/14/21 | §1.6            |
| A8  | S08  | SmartRouter 意图分类 | §1.4            |
| A9  | S09  | 抱抱卡/分享海报         | §1.8            |
| A10 | S10  | 个人中心与我的时光 | §1.10（新增） |
| A11 | S11  | 蜕变广场             | §1.7            |
| A12 | S12  | 个人中心+数据导出+账户注销   | §1.9.3 / §1.9.4 |
| A13 | S13  | 推送调度             | §1.9.2          |
| A14 | S14  | 合规+安全审计          | §3              |
| A15 | S15  | 时光相册(轻量版)        | §1.10           |

---

**PRD V1.2 完**
