# =============================================================================
# SelfwellAgent 根 .gitignore（2026-07-05 W0 初始化 · GitHub Public 策略）
#
# 设计原则：
# 1. GitHub 公开仓只装代码骨架 + 客户端脚手架
#    - docs/ 全部不入 git（PRD / SPEC / ADR / architecture / data / plan）
#    - 含商业敏感信息（KPI / 成本模型 / 用户表 / 合规词库 / 话术）
#    - 业界惯例：GitHub Public 不放商业 docs，docs 走 Notion / 语雀 / 飞书
# 2. .cursorrules / .cursor/ 不入 git（个人 AI 协作工具配置）
# 3. 三个 Python 缓存目录（mypy / pytest / ruff）一律不入
# 4. .github/ 入 git（CI 流水线是项目核心资产；但 docs-ci.yml 要改写，因 docs 不在）
# 5. _transcript_full.txt 不入 git（个人会话转录）
# 6. 客户端私配置不入 git（AppID / 密钥）
# =============================================================================

# ─────────────────────────────────────────────────────────────────────────────
# 1. docs/ 全部不入 git（GitHub Public 策略）
#    - 移到外部：Notion / 语雀 / 飞书云文档
#    - 本地仓库保留 docs/ 用于本地协作，但不入远程
# ─────────────────────────────────────────────────────────────────────────────
docs/
# 例外：根 README.md / LICENSE / CONTRIBUTING.md 不在 docs/ 下，无需例外
# 团队规范文档（Coding-Conventions-Commit）已改为根 CONTRIBUTING.md

# ─────────────────────────────────────────────────────────────────────────────
# 2. 个人 AI 协作工具配置（不入 git）
# ─────────────────────────────────────────────────────────────────────────────
.cursorrules
.cursor/

# ─────────────────────────────────────────────────────────────────────────────
# 3. Python 工具缓存（标准做法）
# ─────────────────────────────────────────────────────────────────────────────
.mypy_cache/
.pytest_cache/
.ruff_cache/
__pycache__/
*.py[cod]
*$py.class
*.so

# ─────────────────────────────────────────────────────────────────────────────
# 4. 虚拟环境
# ─────────────────────────────────────────────────────────────────────────────
.venv/
venv/
env/
ENV/

# ─────────────────────────────────────────────────────────────────────────────
# 5. 个人会话转录（不入 git）
# ─────────────────────────────────────────────────────────────────────────────
_transcript_full.txt
_transcript_*.txt
agent-transcripts/

# ─────────────────────────────────────────────────────────────────────────────
# 6. 系统 / 编辑器临时文件
# ─────────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.vscode/
.idea/
*.iml
*.ipr
*.iws

# ─────────────────────────────────────────────────────────────────────────────
# 7. 环境变量 / 密钥（绝对不能入 git）
# ─────────────────────────────────────────────────────────────────────────────
.env
.env.local
.env.*.local
*.pem
*.key
secrets.yaml
secrets.yml
credentials.json

# ─────────────────────────────────────────────────────────────────────────────
# 8. 构建产物 / 依赖目录
# ─────────────────────────────────────────────────────────────────────────────
node_modules/
dist/
build/
*.egg-info/

# ─────────────────────────────────────────────────────────────────────────────
# 9. 日志 / 临时文件
# ─────────────────────────────────────────────────────────────────────────────
*.log
logs/
tmp/
temp/
*.tmp

# ─────────────────────────────────────────────────────────────────────────────
# 10. 数据 / 大文件 / 模型权重（不应入 git，用 LFS 或云存储）
# ─────────────────────────────────────────────────────────────────────────────
*.pkl
*.h5
*.onnx
*.pt
*.pth
*.bin
*.zip
*.tar.gz
*.sqlite
*.sqlite3
*.db

# ─────────────────────────────────────────────────────────────────────────────
# 11. 客户端脚手架的本地配置（含 AppID / 密钥）
# ─────────────────────────────────────────────────────────────────────────────
client/mp-selfwell/project.private.config.json

# ─────────────────────────────────────────────────────────────────────────────
# 12. 覆盖率报告
# ─────────────────────────────────────────────────────────────────────────────
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover

# ─────────────────────────────────────────────────────────────────────────────
# 13. 后端开发产物
# ─────────────────────────────────────────────────────────────────────────────
backend/app.egg-info/
backend/__pycache__/
backend/**/__pycache__/

# ─────────────────────────────────────────────────────────────────────────────
# 14. ADR 历史备份目录（不入 git，仅本地调试）
# ─────────────────────────────────────────────────────────────────────────────
docs.adr.backup-*/
# 注：当前是 docs/adr.backup-* 模式在原 V1 评估中有提到
# 实际是 docs/adr/ 全部不入，所以不需要单独例外
# 保留此注释以备后续备份目录命名变化时调整